#include<iostream>
using namespace std;
class student{
	// store six student roll in one obj with (array) and (pass obj)
private:
string name;
int roll;
int marks;

public:
	// constructor
 student(){
 int roll= 0;
	int marks = 0;
	string name= " ";	
 }
void set_name(string n);
void set_roll(int r);
void set_marks(int m);

string get_name();
int get_roll();
int get_marks();	
// destructor
 ~student(){
 	cout<<"The program is Exit"<<endl;	
 }
 // This is setter
 void add(student m[3], student xm[3]);
};

void student:: set_name(string n){
	name = n;
}
void student :: set_roll(int r){
	roll = r;
}
void student :: set_marks(int m){
	marks = m;
}
// This is getter
string student :: get_name(){
	return name;
}

int student :: get_roll(){
	return roll;
}
 int student :: get_marks(){
 	return marks;
 }
 // This is function for add two obj with array
 void student:: add(student m[3], student xm[3]){
for(int i=0; i<3; i++){
	marks += m[i].marks + xm[i].marks;
}
 		
	  }
 
int main(){
	string name;
	int marks;
	int roll;
	
	student obj1[3],obj2[3],obj3;
	
	for(int i=0; i<3; i++){
		
	cout<<"[ "<<i<<"]"<<endl;	
	cout<<"Enter the name of student here ";
	cin>>name;
	cout<<endl;
	obj1[i].set_name(name);
	
	cout<<"Enter the roll of student here ";
	cin>>roll;
	cout<<endl;
	obj1[i].set_roll(roll);
	
	cout<<"Enter the marks of student here ";
	cin>>marks;
	cout<<endl;
	obj1[i].set_marks(marks);
}
cout<<endl<<"=================================="<<endl;
for(int j=0; j<3; j++){
	
	cout<<"[ "<<j<<"]"<<endl;
	cout<<"Enter the name of student here ";
	cin>>name;
	cout<<endl;
	obj2[j].set_name(name);
	
	cout<<"Enter the roll of student here ";
	cin>>roll;
	cout<<endl;
	obj2[j].set_roll(roll);
	
	cout<<"Enter the marks of student here ";
	cin>>marks;
	cout<<endl;
	obj2[j].set_marks(marks);
}

obj3.add(obj1, obj2);

cout<<endl;
cout<<obj3.get_marks();
cout<<endl;
}